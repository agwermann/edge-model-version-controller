# Fed Cloud Aggregator

## Setup Virtual environment

```bash
python3 -m venv venv
```

## Build Docker Container

```bash
docker build -t fed-edge-model-version-controller:0.1 .
docker-compose up -d
```
